Link to portfolio site 
https://muthamafrancs.github.io/portfolio/

Refactor HTML: Remove hardcoded project cards and replace them with an empty container <div id="projects-grid"></div>.
Create data.js: Store all your text, links, and project info here.
Update main.js: Write the loop that reads data.js and creates the HTML for the cards.
Setup Formspree: Replace the form action URL.
Add Automations: Add the GitHub fetch script or the Calendly button to show off your process automation skills.